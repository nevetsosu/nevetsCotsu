Next steps:
     Improve the current command responses

     make a search command (practice with autocomplete)

     Rework the Queue to be a Circular Buffer
     add in a clear command
     move command
     skipto command

     lyrics command (requires new API)


For Later:
Rework the multi audio stream muxing ? A single entity that routes the audio streams