Next steps:
     lyrics command (requires new API)
     seek


For Later:
go through and consider what should be exception and what should be return (through regular validation check)
make the queue paged (figure out how to change the pages based on reaction)
description command
Rework the multi audio stream muxing ? A single entity that routes the audio streams

// testing if the pipeline still works with a private DockerHub Repo