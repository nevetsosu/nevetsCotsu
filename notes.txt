Next steps:
     Add the ability to accept search queries

For Later:
Improve the Queue Command

Rework the CopyAsync Command to differentiate from read fails(ffmpeg failed), cancels (interrupt), and write fails (force disconnnnect)
Rework the multi audio stream muxing ? A single entity that routes the audio streams

Remember:
Test the new Enable and Disable Looping in SongQueue