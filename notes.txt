Next steps:
     Configure a proper logger (Serilog)

     Rework the Queue to be a Circular Buffer
     add in a clear command
     move command
     skipto command
     remove command
     info command (information on a queue entry (like who queued it))

     lyrics command (requires new API)


For Later:
description command
Rework the multi audio stream muxing ? A single entity that routes the audio streams

bugs:
arabic titles (that read right to left) will flip entire lines where they are displayed